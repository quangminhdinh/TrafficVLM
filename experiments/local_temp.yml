SOLVER:
  TRAIN:
    MAX_EPOCH: 30
    BATCH_SIZE: 1
    SAVE_INTERVAL: 10
  VAL:
    VAL_INTERVAL: 5
  LOAD_FROM_EPOCH: 30
  
MODEL:
  USE_LOCAL: True
  ENCODE_LOCAL_TEMPORAL: True

DATA:
  FPS: 3
  MAX_OUTPUT_TOKENS: 1024
